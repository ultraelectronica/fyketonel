// Kevlar Lesson 1: expressions, variables, and strings
// Open with: oxygen kevlar/lessons/01-basics.kv
// Run with Ctrl+Enter.

print("Lesson 1: basics")

let fuel = 12
let bonus = 3
let total = fuel + bonus * 2
print(total)

let pilot = "Orpheus"
print("Pilot: ${pilot}")

let tools = ["torch", "rope", "flare"]
print(len(tools))
print(tools[0])

// Drill 1:
// Change fuel and bonus, then run again.

// Drill 2:
// Uncomment the next two lines.
// push(tools, "mask")
// print(len(tools))

// Drill 3:
// Create let station = "bay-12" and print it.
